# You can use lambda functions in the sort() method as in the filter() and map() functions
# Lambda is an anonymous one time function
# Like a function, it can take a parameter and return a result

x = lambda a: a + 10
print(x(5))
print(type(x))